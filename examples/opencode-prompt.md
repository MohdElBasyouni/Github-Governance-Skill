# OpenCode Prompt Example

```text
Use the github-governance-skill workflow from ~/.agents/skills/github-governance-skill for all git operations in this repository.

Create a compliant working branch before changes, never push to main or master, run discovered checks before push, use configured review tools, verify PR checks are green, address actionable review comments, and stop for Mohamed's explicit consent before any merge or destructive action.
```
